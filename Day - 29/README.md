## Day 29 – Environment Variable Validator

This script checks whether required environment variables are set.

Why this is useful:
Prevents application failures caused by missing configuration values.

Usage:
./day-29-env-check.sh




Example Output

If all set:

DB_HOST is set
DB_USER is set
DB_PASS is set
API_KEY is set

All required environment variables are set